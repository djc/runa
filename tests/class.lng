class xrange:
	
	start: int
	stop: int
	step: int
	
	def __init__(self: xrange, start: int, stop: int, step: int):
		self.start = start
		self.stop = stop
		self.step = step

def main(name: str, args: list[str]) -> int:
	r = xrange(0, 5, 1)
	print(str(r.start))
	print(str(r.stop))
	print(str(r.step))
